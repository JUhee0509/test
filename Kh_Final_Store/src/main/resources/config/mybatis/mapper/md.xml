<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="m">
	<select id="md_list" resultType="vo.ProductVO">
    SELECT product.s_image, product.p_name, product.brand, product.price, product.p_idx
	from md, product
	where md.p_idx = product.p_idx
	</select>
	
</mapper>












